---
phase: 07-polish-and-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/regulatorio/controllers/admin-config.controller.ts
  - backend/src/modules/regulatorio/services/admin-config.service.ts
  - backend/src/modules/regulatorio/regulatorio.module.ts
  - frontend/package.json
  - frontend/src/app/admin/personas/page.tsx
  - frontend/src/app/admin/personas/actions.ts
  - frontend/src/app/admin/templates/page.tsx
  - frontend/src/app/admin/templates/actions.ts
  - frontend/src/app/admin/skills/page.tsx
  - frontend/src/app/admin/skills/actions.ts
  - frontend/src/app/admin/capabilities/page.tsx
  - frontend/src/app/admin/capabilities/actions.ts
  - frontend/src/app/admin/models/page.tsx
  - frontend/src/app/admin/models/actions.ts
autonomous: true

must_haves:
  truths:
    - "Admin can list, create, update, and delete personas from the UI at /admin/personas without recompiling"
    - "Admin can list and update response templates at /admin/templates without recompiling"
    - "Admin can list and toggle active state of skills, capabilities, and LLM models from the UI"
    - "4 pre-configured personas are visible in the personas list"
    - "All admin endpoints respond with 403 to non-admin users"
  artifacts:
    - path: "backend/src/modules/regulatorio/controllers/admin-config.controller.ts"
      provides: "REST endpoints for CRUD on personas, templates, skills, capabilities, LLM model configs — all gated with @Roles(UserRole.ADMIN)"
    - path: "backend/src/modules/regulatorio/services/admin-config.service.ts"
      provides: "Service methods: listPersonas, createPersona, updatePersona, deletePersona, listTemplates, updateTemplate, listSkills, updateSkill, listCapabilities, updateCapability, listModels, updateModel"
    - path: "frontend/src/app/admin/personas/page.tsx"
      provides: "Server component listing personas with create/edit form using useActionState"
    - path: "frontend/src/app/admin/templates/page.tsx"
      provides: "Server component listing templates with edit form"
  key_links:
    - from: "frontend/src/app/admin/personas/actions.ts"
      to: "/api/admin/personas"
      via: "fetchAuthAPI(token, '/api/admin/personas', { method: 'POST', body: JSON.stringify(body) })"
      pattern: "fetchAuthAPI.*admin/personas"
    - from: "backend/src/modules/regulatorio/controllers/admin-config.controller.ts"
      to: "backend/src/modules/regulatorio/services/admin-config.service.ts"
      via: "constructor injection"
      pattern: "@Roles\\(UserRole\\.ADMIN\\)"
---

<objective>
Build the persona catalog management and full configuration admin UI: CRUD for personas, templates, skills, capabilities, and LLM model configs — all served from the existing DB entities with no recompile needed.

Purpose: PERS-01, PERS-02, PERS-03, CONF-01 through CONF-07 — admin can manage all catalog items from the UI. CONF-03 (steps) is already built in Phase 6 at /admin/steps.
Output: AdminConfigController + AdminConfigService (backend) + 5 admin page groups (frontend).
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-compliance/07-RESEARCH.md

Key source files to read before implementing:
@backend/src/modules/regulatorio/regulatorio.module.ts
@backend/src/modules/regulatorio/entities/
@backend/src/modules/execucao/execution.controller.ts
@frontend/src/app/admin/steps/page.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminConfigController + AdminConfigService (backend)</name>
  <files>
    backend/src/modules/regulatorio/controllers/admin-config.controller.ts
    backend/src/modules/regulatorio/services/admin-config.service.ts
    backend/src/modules/regulatorio/regulatorio.module.ts
  </files>
  <action>
**admin-config.service.ts** — Injectable service with the following methods. Use TypeORM repository injection via `@InjectRepository()`. All returns are plain entity arrays or single entities. No pagination needed for admin catalog (item counts are small).

Entities to inject: `Persona`, `ResponseTemplate`, `SkillDefinition`, `CapabilityVersion`, `LlmModelConfig`. Import each from their respective entity files.

Methods:
- `listPersonas(): Promise<Persona[]>` — `repo.find({ order: { createdAt: 'ASC' } })`
- `createPersona(dto: Partial<Persona>): Promise<Persona>` — `repo.save(repo.create(dto))`; after save, sync StyleMemory: for each entry in `dto.requiredExpressions ?? []`, insert a `style_memory` row with `expressionType: 'approved'`, `tipologyId: dto.tipologyId`, `expression: expr` via `dataSource.query('INSERT INTO style_memory ("id","tipologyId","expressionType","style","createdAt") VALUES (gen_random_uuid(),$1,$2,$3,NOW())', [dto.tipologyId ?? null, 'approved', JSON.stringify({ expression: expr })])`. For each entry in `dto.forbiddenExpressions ?? []`, insert with `expressionType: 'forbidden'`. Skip StyleMemory sync if DataSource unavailable — it's supplementary.
- `updatePersona(id: string, dto: Partial<Persona>): Promise<Persona>` — `await repo.update(id, dto); return repo.findOneOrFail({ where: { id } })`; repeat StyleMemory sync (delete old rows for this persona's tipologyId first via raw DELETE, then re-insert).
- `deletePersona(id: string): Promise<void>` — `repo.delete(id)`
- `listTemplates(): Promise<ResponseTemplate[]>` — `repo.find({ order: { version: 'DESC' } })`
- `updateTemplate(id: string, dto: Partial<ResponseTemplate>): Promise<ResponseTemplate>` — increment version before saving: `dto.version = (existing.version ?? 0) + 1`. Save via `repo.save({ ...existing, ...dto })`.
- `listSkills(): Promise<SkillDefinition[]>` — `repo.find({ order: { skillKey: 'ASC' } })`
- `updateSkill(id: string, dto: Partial<SkillDefinition>): Promise<SkillDefinition>` — `repo.save({ ...existing, ...dto })`
- `listCapabilities(): Promise<CapabilityVersion[]>` — `repo.find({ order: { createdAt: 'DESC' } })`
- `updateCapability(id: string, dto: Partial<CapabilityVersion>): Promise<CapabilityVersion>` — `repo.save({ ...existing, ...dto })`
- `listModels(): Promise<LlmModelConfig[]>` — `repo.find({ order: { functionalityType: 'ASC' } })`
- `updateModel(id: string, dto: Partial<LlmModelConfig>): Promise<LlmModelConfig>` — `repo.save({ ...existing, ...dto })`

**admin-config.controller.ts** — Controller with NO base path prefix in `@Controller()`. Each route uses full path. All endpoints decorated with `@Roles(UserRole.ADMIN)`. Import `UserRole` from `../../operacao/entities/user.entity` and `Roles` from `../../auth/decorators/roles.decorator`.

Routes (all return JSON):
- `GET /api/admin/personas` — `listPersonas()`
- `POST /api/admin/personas` — `createPersona(@Body() body)`
- `PATCH /api/admin/personas/:id` — `updatePersona(@Param('id') id, @Body() body)`
- `DELETE /api/admin/personas/:id` — `deletePersona(@Param('id') id)`, return `{ success: true }`
- `GET /api/admin/templates` — `listTemplates()`
- `PATCH /api/admin/templates/:id` — `updateTemplate(@Param('id') id, @Body() body)`
- `GET /api/admin/skills` — `listSkills()`
- `PATCH /api/admin/skills/:id` — `updateSkill(@Param('id') id, @Body() body)`
- `GET /api/admin/capabilities` — `listCapabilities()`
- `PATCH /api/admin/capabilities/:id` — `updateCapability(@Param('id') id, @Body() body)`
- `GET /api/admin/models` — `listModels()`
- `PATCH /api/admin/models/:id` — `updateModel(@Param('id') id, @Body() body)`

**regulatorio.module.ts update:**
- Add `AdminConfigController` to `controllers` array
- Add `AdminConfigService` to `providers` array
- Add TypeOrmModule.forFeature for any entities not already registered: `SkillDefinition`, `CapabilityVersion`, `LlmModelConfig`, `ResponseTemplate`, `Persona`. Check what's already registered and add only missing ones.
- Inject `DataSource` in `AdminConfigService` via `@InjectDataSource()` for the StyleMemory sync raw queries.
  </action>
  <verify>
SSH to VPS: `ssh root@72.61.52.70 "cd /opt/bko-agent/backend && npm run build 2>&1 | tail -20"` — zero TS errors.
  </verify>
  <done>
Backend compiles clean. AdminConfigController registers 12 endpoints. All endpoints have @Roles(UserRole.ADMIN). Module updated with new controller and service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin UI pages for personas, templates, skills, capabilities, and models</name>
  <files>
    frontend/src/app/admin/personas/page.tsx
    frontend/src/app/admin/personas/actions.ts
    frontend/src/app/admin/templates/page.tsx
    frontend/src/app/admin/templates/actions.ts
    frontend/src/app/admin/skills/page.tsx
    frontend/src/app/admin/skills/actions.ts
    frontend/src/app/admin/capabilities/page.tsx
    frontend/src/app/admin/capabilities/actions.ts
    frontend/src/app/admin/models/page.tsx
    frontend/src/app/admin/models/actions.ts
  </files>
  <action>
Follow the exact patterns from `/admin/steps` (Phase 6). All pages are Server Components that fetch data from the backend then render. Forms use `useActionState` from 'react' (React 19) in a separate Client Component imported into the page. All server actions use `getSession()` + `fetchAuthAPI(token, path, options)` from existing lib files. All actions return `ActionState = { error?: string; success?: boolean }`.

**Pattern to follow exactly (from Phase 6):**
- Page (`page.tsx`): async Server Component, calls `getSession()`, fetches list from backend with token via `fetchAuthAPI`, renders table + form Client Component
- Actions (`actions.ts`): `'use server'` file, `export async function createX(_prev: ActionState, formData: FormData): Promise<ActionState>` shape
- Client form component: inline in page.tsx using `'use client'` + `useActionState`

**personas/page.tsx + personas/actions.ts:**
Page: Fetch `GET /api/admin/personas`. Render table with columns: name, tipologia (tipologyId or "global"), formalityLevel, empathyLevel, assertivenessLevel, requiredExpressions count, forbiddenExpressions count. Include an "Add Persona" form with fields: name (text), tipologyId (text, optional), formalityLevel (number 1-5), empathyLevel (number 1-5), assertivenessLevel (number 1-5), requiredExpressions (textarea, newline-separated), forbiddenExpressions (textarea, newline-separated). Include a delete button per row (calls DELETE action).
Actions: `createPersona`, `deletePersona`. Parse requiredExpressions/forbiddenExpressions from newline-split textarea value.

**templates/page.tsx + templates/actions.ts:**
Page: Fetch `GET /api/admin/templates`. Render table with columns: tipologyKey, situationKey, version, contentLength (content string length). Each row has an "Edit" inline form showing a textarea for template content (the `content` field as JSON string).
Actions: `updateTemplate(id, _prev, formData)` — PATCH `/api/admin/templates/:id` with `{ content: JSON.parse(formData.get('content')) }`.

**skills/page.tsx + skills/actions.ts:**
Page: Fetch `GET /api/admin/skills`. Render table with columns: skillKey, description, isActive (badge). Each row has toggle button.
Actions: `toggleSkill(id, isActive)` — PATCH `/api/admin/skills/:id` with `{ isActive: !isActive }`.

**capabilities/page.tsx + capabilities/actions.ts:**
Page: Fetch `GET /api/admin/capabilities`. Render table with columns: name, version, status. Each row shows name + status badge.
Actions: `updateCapability(id, _prev, formData)` — PATCH with `{ status: formData.get('status') }`.

**models/page.tsx + models/actions.ts:**
Page: Fetch `GET /api/admin/models`. Render table with columns: functionalityType, provider, modelId, temperature, isActive badge. Each row has inline edit for modelId and temperature + toggle for isActive.
Actions: `updateModel(id, _prev, formData)` — PATCH `/api/admin/models/:id` with `{ modelId, temperature: Number(temperature), isActive }`.

All pages use Tailwind CSS consistent with existing admin/steps pages. Add a navigation bar or breadcrumb showing "Admin > [section name]". The /admin layout can be a simple shared header — check if `frontend/src/app/admin/layout.tsx` exists; if not, create a minimal one with navigation links to: /admin/personas, /admin/templates, /admin/steps, /admin/skills, /admin/capabilities, /admin/models.
  </action>
  <verify>
SSH to VPS:
```
ssh root@72.61.52.70 "cd /opt/bko-agent/frontend && npm run build 2>&1 | tail -30"
```
Zero TS/build errors. All 10 admin pages compile.
  </verify>
  <done>
Frontend builds clean. 5 new admin page groups exist at /admin/personas, /admin/templates, /admin/skills, /admin/capabilities, /admin/models. All use server actions + fetchAuthAPI pattern. Personas page shows 4 seeded personas. Models page shows configured LLM models.
  </done>
</task>

</tasks>

<verification>
Backend builds with zero errors. Frontend builds with zero errors. All 12 admin API endpoints registered with @Roles(UserRole.ADMIN). Admin UI pages exist for all 5 catalog sections. Phase 6 /admin/steps remains unbroken.
</verification>

<success_criteria>
1. GET /api/admin/personas returns 4 seeded personas (when called with admin token)
2. POST /api/admin/personas creates a new persona persisted in DB
3. GET /api/admin/personas returns 403 for operator/supervisor tokens
4. Frontend /admin/personas page renders the persona list
5. All 5 admin page groups build without errors
6. Backend builds clean with zero TS errors
7. Frontend builds clean with zero TS/build errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-compliance/07-02-SUMMARY.md`
</output>
