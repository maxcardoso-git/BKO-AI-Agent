---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/modules/operacao/entities/complaint.entity.ts
  - backend/src/modules/operacao/entities/complaint-detail.entity.ts
  - backend/src/modules/operacao/entities/complaint-history.entity.ts
  - backend/src/modules/operacao/entities/complaint-attachment.entity.ts
  - backend/src/modules/regulatorio/entities/tipology.entity.ts
  - backend/src/modules/regulatorio/entities/subtipology.entity.ts
  - backend/src/modules/regulatorio/entities/situation.entity.ts
  - backend/src/modules/regulatorio/entities/regulatory-rule.entity.ts
  - backend/src/modules/regulatorio/entities/regulatory-action.entity.ts
  - backend/src/modules/regulatorio/entities/persona.entity.ts
  - backend/src/modules/regulatorio/entities/response-template.entity.ts
  - backend/src/modules/regulatorio/entities/mandatory-info-rule.entity.ts
  - backend/src/modules/orquestracao/entities/capability.entity.ts
  - backend/src/modules/orquestracao/entities/capability-version.entity.ts
  - backend/src/modules/orquestracao/entities/step-definition.entity.ts
  - backend/src/modules/orquestracao/entities/step-transition-rule.entity.ts
  - backend/src/modules/orquestracao/entities/skill-definition.entity.ts
  - backend/src/modules/orquestracao/entities/step-skill-binding.entity.ts
  - backend/src/modules/execucao/entities/ticket-execution.entity.ts
  - backend/src/modules/execucao/entities/step-execution.entity.ts
  - backend/src/modules/execucao/entities/artifact.entity.ts
  - backend/src/modules/execucao/entities/llm-call.entity.ts
  - backend/src/modules/execucao/entities/token-usage.entity.ts
  - backend/src/modules/execucao/entities/human-review.entity.ts
  - backend/src/modules/execucao/entities/audit-log.entity.ts
  - backend/src/modules/memoria/entities/kb-document.entity.ts
  - backend/src/modules/memoria/entities/kb-document-version.entity.ts
  - backend/src/modules/memoria/entities/kb-chunk.entity.ts
  - backend/src/modules/memoria/entities/case-memory.entity.ts
  - backend/src/modules/memoria/entities/human-feedback-memory.entity.ts
  - backend/src/modules/memoria/entities/style-memory.entity.ts
  - backend/src/modules/operacao/operacao.module.ts
  - backend/src/modules/regulatorio/regulatorio.module.ts
  - backend/src/modules/orquestracao/orquestracao.module.ts
  - backend/src/modules/execucao/execucao.module.ts
  - backend/src/modules/memoria/memoria.module.ts
autonomous: true

must_haves:
  truths:
    - "All ~31 tables exist in PostgreSQL across 5 domains after migration"
    - "pgvector columns exist on kb_chunk, case_memory, human_feedback_memory, style_memory"
    - "Foreign key relationships are correctly defined between domains"
    - "UUID primary keys are used on all tables"
    - "All domain modules export TypeOrmModule for cross-module entity access"
  artifacts:
    - path: "backend/src/modules/operacao/entities/complaint.entity.ts"
      provides: "Main complaint entity"
      contains: "@Entity('complaint')"
    - path: "backend/src/modules/regulatorio/entities/tipology.entity.ts"
      provides: "Tipology lookup entity"
      contains: "@Entity('tipology')"
    - path: "backend/src/modules/memoria/entities/kb-chunk.entity.ts"
      provides: "Knowledge base chunk with vector embedding"
      contains: "vector"
    - path: "backend/src/modules/execucao/entities/audit-log.entity.ts"
      provides: "Append-only audit trail"
      contains: "@Entity('audit_log')"
  key_links:
    - from: "complaint.entity.ts"
      to: "tipology.entity.ts"
      via: "ManyToOne relation"
      pattern: "ManyToOne.*Tipology"
    - from: "step-execution.entity.ts"
      to: "ticket-execution.entity.ts"
      via: "ManyToOne relation"
      pattern: "ManyToOne.*TicketExecution"
    - from: "kb-chunk.entity.ts"
      to: "kb-document.entity.ts"
      via: "ManyToOne relation"
      pattern: "ManyToOne.*KbDocument"
    - from: "Each domain module"
      to: "TypeOrmModule"
      via: "exports: [TypeOrmModule]"
      pattern: "exports.*TypeOrmModule"
---

<objective>
Define all 31 TypeORM entities across 5 domains and generate migration files so the complete database schema exists in PostgreSQL.

Purpose: The entire platform (auth, orchestration, AI, skills, HITL) depends on these tables existing. This plan creates every entity with correct columns, types, relations, and constraints so all subsequent phases have a stable schema to build on.
Output: 31 entity files organized by domain module, domain-scoped TypeORM migration files, all tables created in PostgreSQL.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 31 entity definitions across 5 domain modules</name>
  <files>
    backend/src/modules/operacao/entities/complaint.entity.ts
    backend/src/modules/operacao/entities/complaint-detail.entity.ts
    backend/src/modules/operacao/entities/complaint-history.entity.ts
    backend/src/modules/operacao/entities/complaint-attachment.entity.ts
    backend/src/modules/regulatorio/entities/tipology.entity.ts
    backend/src/modules/regulatorio/entities/subtipology.entity.ts
    backend/src/modules/regulatorio/entities/situation.entity.ts
    backend/src/modules/regulatorio/entities/regulatory-rule.entity.ts
    backend/src/modules/regulatorio/entities/regulatory-action.entity.ts
    backend/src/modules/regulatorio/entities/persona.entity.ts
    backend/src/modules/regulatorio/entities/response-template.entity.ts
    backend/src/modules/regulatorio/entities/mandatory-info-rule.entity.ts
    backend/src/modules/orquestracao/entities/capability.entity.ts
    backend/src/modules/orquestracao/entities/capability-version.entity.ts
    backend/src/modules/orquestracao/entities/step-definition.entity.ts
    backend/src/modules/orquestracao/entities/step-transition-rule.entity.ts
    backend/src/modules/orquestracao/entities/skill-definition.entity.ts
    backend/src/modules/orquestracao/entities/step-skill-binding.entity.ts
    backend/src/modules/execucao/entities/ticket-execution.entity.ts
    backend/src/modules/execucao/entities/step-execution.entity.ts
    backend/src/modules/execucao/entities/artifact.entity.ts
    backend/src/modules/execucao/entities/llm-call.entity.ts
    backend/src/modules/execucao/entities/token-usage.entity.ts
    backend/src/modules/execucao/entities/human-review.entity.ts
    backend/src/modules/execucao/entities/audit-log.entity.ts
    backend/src/modules/memoria/entities/kb-document.entity.ts
    backend/src/modules/memoria/entities/kb-document-version.entity.ts
    backend/src/modules/memoria/entities/kb-chunk.entity.ts
    backend/src/modules/memoria/entities/case-memory.entity.ts
    backend/src/modules/memoria/entities/human-feedback-memory.entity.ts
    backend/src/modules/memoria/entities/style-memory.entity.ts
    backend/src/modules/operacao/operacao.module.ts
    backend/src/modules/regulatorio/regulatorio.module.ts
    backend/src/modules/orquestracao/orquestracao.module.ts
    backend/src/modules/execucao/execucao.module.ts
    backend/src/modules/memoria/memoria.module.ts
  </files>
  <action>
    Create all 31 TypeORM entity files following the domain structure from RESEARCH.md Entity Map. ALL entities use UUID primary keys via @PrimaryGeneratedColumn('uuid'). ALL entities have createdAt and updatedAt timestamps via @CreateDateColumn() and @UpdateDateColumn().

    **DOMAIN: Operacao (4 entities)**

    `complaint.entity.ts`:
    - id (uuid PK), protocolNumber (varchar unique — Anatel protocol), rawText (text — original complaint text), normalizedText (text nullable — cleaned version), status (enum: pending, in_progress, waiting_human, completed, cancelled), riskLevel (enum: low, medium, high, critical), slaDeadline (timestamp nullable), slaBusinessDays (int nullable), isOverdue (boolean default false), source (varchar — origin system e.g. "anatel_portal"), externalId (varchar nullable — ID in source system), procedente (boolean nullable — founded/unfounded), createdAt, updatedAt
    - Relations: ManyToOne to Tipology (nullable — set after classification), ManyToOne to Subtipology (nullable), ManyToOne to Situation (nullable), ManyToOne to RegulatoryAction (nullable — set after action decision)
    - OneToMany to ComplaintDetail, ComplaintHistory, ComplaintAttachment

    `complaint-detail.entity.ts`:
    - id (uuid PK), fieldName (varchar — e.g. "customer_name", "phone_number", "plan_name"), fieldValue (text), fieldType (varchar — "text", "date", "number", "boolean"), confidence (float nullable — extraction confidence 0-1), source (varchar — "parsed", "manual", "enriched"), createdAt, updatedAt
    - ManyToOne to Complaint

    `complaint-history.entity.ts`:
    - id (uuid PK), action (varchar — "created", "classified", "step_completed", "human_reviewed", "finalized", etc.), description (text nullable), previousStatus (varchar nullable), newStatus (varchar nullable), performedBy (varchar nullable — user ID or "system"), metadata (jsonb nullable — extra data about the action), createdAt
    - ManyToOne to Complaint

    `complaint-attachment.entity.ts`:
    - id (uuid PK), fileName (varchar), fileType (varchar — mime type), fileSize (int), storagePath (varchar — local path or S3 key), uploadedBy (varchar nullable), createdAt
    - ManyToOne to Complaint

    **DOMAIN: Regulatorio (8 entities)**

    `tipology.entity.ts`:
    - id (uuid PK), key (varchar unique — "cobranca", "cancelamento", "portabilidade", "qualidade"), label (varchar — display name), description (text nullable), slaBusinessDays (int — default SLA for this tipology), isActive (boolean default true), createdAt, updatedAt

    `subtipology.entity.ts`:
    - id (uuid PK), key (varchar unique), label (varchar), description (text nullable), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology

    `situation.entity.ts`:
    - id (uuid PK), key (varchar unique — "aberta", "reaberta", "vencida", "em_risco", "pedido"), label (varchar), slaOverrideDays (int nullable — overrides tipology SLA if set, e.g. 5 for reaberta, 3 for pedido), description (text nullable), isActive (boolean default true), createdAt, updatedAt

    `regulatory-rule.entity.ts`:
    - id (uuid PK), code (varchar unique — rule identifier), title (varchar), description (text), sourceDocument (varchar — "manual_anatel", "guia_iqi", etc.), sourceSection (varchar nullable), ruleType (enum: "sla", "mandatory_field", "action_condition", "blocking", "informational"), isActive (boolean default true), metadata (jsonb nullable), createdAt, updatedAt
    - ManyToOne to Tipology (nullable — null means applies to all), ManyToOne to Situation (nullable)

    `regulatory-action.entity.ts`:
    - id (uuid PK), key (varchar unique — "responder", "reclassificar", "reencaminhar", "cancelar"), label (varchar), description (text nullable), requiresJustification (boolean default false), isActive (boolean default true), createdAt, updatedAt

    `persona.entity.ts`:
    - id (uuid PK), name (varchar), description (text nullable), formalityLevel (int — 1 to 5), empathyLevel (int — 1 to 5), assertivenessLevel (int — 1 to 5), requiredExpressions (text[] nullable — array of strings that must appear), forbiddenExpressions (text[] nullable — array of strings that must not appear), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology (nullable — null means generic persona)

    `response-template.entity.ts`:
    - id (uuid PK), name (varchar), templateContent (text — the template body with placeholders), version (int default 1), sourceDocument (varchar nullable — e.g. "guia_iqi"), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology (nullable), ManyToOne to Situation (nullable)

    `mandatory-info-rule.entity.ts`:
    - id (uuid PK), fieldName (varchar — required field name), fieldLabel (varchar — display label), description (text nullable), validationRule (varchar nullable — regex or rule expression), isRequired (boolean default true), sortOrder (int default 0), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology (nullable), ManyToOne to Situation (nullable)

    **DOMAIN: Orquestracao (6 entities)**

    `capability.entity.ts`:
    - id (uuid PK), key (varchar unique — identifier), name (varchar), description (text nullable), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology (nullable — a capability may be generic or tipology-specific)
    - OneToMany to CapabilityVersion

    `capability-version.entity.ts`:
    - id (uuid PK), version (int), description (text nullable), isActive (boolean default true), isCurrent (boolean default false), createdAt, updatedAt
    - ManyToOne to Capability
    - OneToMany to StepDefinition

    `step-definition.entity.ts`:
    - id (uuid PK), key (varchar), name (varchar), description (text nullable), stepOrder (int — execution order), isHumanRequired (boolean default false — flag for HITL steps), timeoutSeconds (int nullable), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to CapabilityVersion

    `step-transition-rule.entity.ts`:
    - id (uuid PK), conditionType (varchar — "sla_risk", "procedencia", "risk_level", "situation", "custom"), conditionExpression (jsonb — the rule expression), targetStepKey (varchar — step key to transition to), priority (int default 0 — higher = checked first), description (text nullable), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to StepDefinition (the step this transition applies FROM)

    `skill-definition.entity.ts`:
    - id (uuid PK), key (varchar unique — e.g. "LoadComplaint", "ClassifyTypology"), name (varchar), description (text nullable), inputSchema (jsonb nullable — expected input shape), outputSchema (jsonb nullable — expected output shape), version (varchar default "1.0.0"), isActive (boolean default true), createdAt, updatedAt

    `step-skill-binding.entity.ts`:
    - id (uuid PK), configuration (jsonb nullable — skill-specific config for this binding), llmModel (varchar nullable — model override for this step+skill), promptVersion (varchar nullable), sortOrder (int default 0 — order of skills within a step), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to StepDefinition, ManyToOne to SkillDefinition

    **DOMAIN: Execucao (7 entities)**

    `ticket-execution.entity.ts`:
    - id (uuid PK), status (enum: "pending", "running", "paused_human", "completed", "failed", "cancelled"), startedAt (timestamp nullable), completedAt (timestamp nullable), currentStepKey (varchar nullable), totalDurationMs (int nullable), errorMessage (text nullable), metadata (jsonb nullable), createdAt, updatedAt
    - ManyToOne to Complaint, ManyToOne to CapabilityVersion
    - OneToMany to StepExecution

    `step-execution.entity.ts`:
    - id (uuid PK), stepKey (varchar), status (enum: "pending", "running", "completed", "failed", "skipped", "waiting_human"), startedAt (timestamp nullable), completedAt (timestamp nullable), durationMs (int nullable), input (jsonb nullable), output (jsonb nullable), errorMessage (text nullable), retryCount (int default 0), createdAt, updatedAt
    - ManyToOne to TicketExecution, ManyToOne to StepDefinition (nullable)
    - OneToMany to Artifact, OneToMany to LlmCall

    `artifact.entity.ts`:
    - id (uuid PK), artifactType (varchar — e.g. "parsed_complaint", "classification_result", "sla_calculation", "regulatory_action", "manual_context", "iqi_template", "mandatory_checklist", "generated_artifact", "persona_response", "draft_response", "compliance_report"), content (jsonb — artifact payload), version (int default 1), createdAt
    - ManyToOne to StepExecution, ManyToOne to Complaint

    `llm-call.entity.ts`:
    - id (uuid PK), model (varchar — model identifier), provider (varchar — "openai", "anthropic", etc.), promptTokens (int), completionTokens (int), totalTokens (int), costUsd (decimal(10,6) nullable), latencyMs (int nullable), promptHash (varchar nullable — hash of prompt for dedup), responseStatus (varchar — "success", "error", "timeout"), errorMessage (text nullable), createdAt
    - ManyToOne to StepExecution
    - OneToOne to TokenUsage (nullable)

    `token-usage.entity.ts`:
    - id (uuid PK), promptTokens (int), completionTokens (int), totalTokens (int), costUsd (decimal(10,6)), model (varchar), createdAt
    - OneToOne to LlmCall (with JoinColumn)

    `human-review.entity.ts`:
    - id (uuid PK), reviewerUserId (varchar — user who reviewed), status (enum: "pending", "approved", "rejected", "revision_requested"), aiGeneratedText (text — original AI output), humanFinalText (text nullable — human-edited version), diffSummary (text nullable — summary of changes), correctionReason (text nullable — why human changed it), checklistCompleted (boolean default false), checklistItems (jsonb nullable — regulatory checklist state), observations (text nullable), reviewedAt (timestamp nullable), createdAt, updatedAt
    - ManyToOne to StepExecution, ManyToOne to Complaint

    `audit-log.entity.ts`:
    - id (uuid PK), action (varchar — "complaint.created", "step.started", "human.approved", etc.), entityType (varchar — "complaint", "step_execution", "human_review", etc.), entityId (varchar — UUID of the referenced entity), userId (varchar nullable), details (jsonb nullable), ipAddress (varchar nullable), createdAt
    - NO updatedAt — audit logs are append-only, never updated

    **DOMAIN: Memoria (6 entities)**

    `kb-document.entity.ts`:
    - id (uuid PK), title (varchar), sourceType (varchar — "manual_anatel", "guia_iqi", "planilha", "other"), filePath (varchar nullable), mimeType (varchar nullable), isActive (boolean default true), createdAt, updatedAt
    - OneToMany to KbDocumentVersion

    `kb-document-version.entity.ts`:
    - id (uuid PK), version (int), changeDescription (text nullable), processedAt (timestamp nullable), chunkCount (int default 0), isActive (boolean default true), createdAt
    - ManyToOne to KbDocument
    - OneToMany to KbChunk

    `kb-chunk.entity.ts`:
    - id (uuid PK), content (text), chunkIndex (int — order within document), sectionTitle (varchar nullable), metadata (jsonb nullable — section, page, etc.), embedding (vector(1536) — pgvector column, see RESEARCH Pattern 2), createdAt
    - ManyToOne to KbDocumentVersion

    `case-memory.entity.ts`:
    - id (uuid PK), summary (text — case summary), decision (text nullable — what was decided), outcome (varchar nullable — "procedente", "improcedente", "reclassificado", etc.), responseSnippet (text nullable — final response excerpt), embedding (vector(1536) — for similarity search), isActive (boolean default true), createdAt
    - ManyToOne to Complaint, ManyToOne to Tipology (nullable)

    `human-feedback-memory.entity.ts`:
    - id (uuid PK), aiText (text), humanText (text), diffDescription (text nullable), correctionCategory (varchar nullable — "tone", "factual", "regulatory", "formatting", "other"), correctionWeight (float default 1.0 — importance for learning), embedding (vector(1536) — for similarity on correction patterns), createdAt
    - ManyToOne to Complaint (nullable), ManyToOne to Tipology (nullable)

    `style-memory.entity.ts`:
    - id (uuid PK), expressionText (text — the expression or pattern), expressionType (enum: "approved", "forbidden"), context (text nullable — when this applies), source (varchar nullable — who approved/forbade it), isActive (boolean default true), createdAt, updatedAt
    - ManyToOne to Tipology (nullable — null means global)

    **After all entities are created:**

    Update each domain module to register its entities via TypeOrmModule.forFeature([...entities]) and export TypeOrmModule (RESEARCH Pattern 3 — critical for cross-module access, see pitfall 5).
  </action>
  <verify>
    - `cd backend && npm run build` compiles all 31 entity files without errors
    - `find backend/src/modules -name "*.entity.ts" | wc -l` returns 31
    - `grep -r "exports.*TypeOrmModule" backend/src/modules/` returns 5 results (one per domain module)
    - `grep -r "synchronize.*true" backend/src/` returns NO results
  </verify>
  <done>
    All 31 entity files exist across 5 domain modules. Each entity has UUID PK, timestamps, correct relations. All domain modules register and export their entities. Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and run TypeORM migrations to create all tables in PostgreSQL</name>
  <files>
    backend/src/database/migrations/ (new migration files — one per domain)
  </files>
  <action>
    With all 31 entities defined and the backend compiling, generate TypeORM migrations. Write domain-scoped migration files manually (do NOT use migration:generate which creates a single monolithic file — see RESEARCH anti-pattern).

    Create 5 migration files (numbered to run in dependency order):

    1. `001-CreateOperacaoTables.ts` — complaint, complaint_detail, complaint_history, complaint_attachment
    2. `002-CreateRegulatorioTables.ts` — tipology, subtipology, situation, regulatory_rule, regulatory_action, persona, response_template, mandatory_info_rule
    3. `003-CreateOrquestracaoTables.ts` — capability, capability_version, step_definition, step_transition_rule, skill_definition, step_skill_binding
    4. `004-CreateExecucaoTables.ts` — ticket_execution, step_execution, artifact, llm_call, token_usage, human_review, audit_log
    5. `005-CreateMemoriaTables.ts` — kb_document, kb_document_version, kb_chunk, case_memory, human_feedback_memory, style_memory

    Each migration uses raw SQL in `queryRunner.query()` for full control. Include:
    - CREATE TABLE with all columns and correct types
    - Primary keys (uuid with DEFAULT uuid_generate_v4())
    - Foreign key constraints with ON DELETE CASCADE or SET NULL as appropriate
    - Indexes on frequently queried columns (complaint.status, complaint.protocolNumber, tipology.key, situation.key, etc.)
    - The vector(1536) column type for embedding fields in memoria tables (extension already enabled by migration 000)

    The `down()` method should DROP TABLES in reverse order within each migration.

    After writing migrations, run them:
    ```bash
    cd backend && npm run build && npx typeorm migration:run -d dist/database/data-source.js
    ```

    Verify all tables exist:
    ```sql
    SELECT table_name FROM information_schema.tables WHERE table_schema='public' ORDER BY table_name;
    ```
  </action>
  <verify>
    - `npm run migration:run` completes without errors
    - `docker compose exec postgres psql -U bko -d bkoagent -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_type='BASE TABLE'"` returns >= 31 (31 domain tables + migrations table)
    - `docker compose exec postgres psql -U bko -d bkoagent -c "SELECT column_name, data_type FROM information_schema.columns WHERE table_name='kb_chunk' AND column_name='embedding'"` returns "embedding | USER-DEFINED" (pgvector type)
    - `docker compose exec postgres psql -U bko -d bkoagent -c "\dt"` shows all expected tables
  </verify>
  <done>
    All ~31 tables exist in PostgreSQL across 5 domains. Foreign keys connect entities correctly. pgvector columns exist on kb_chunk, case_memory, human_feedback_memory. UUID primary keys and timestamps on all tables. Migrations are versioned and repeatable.
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npm run build` compiles without errors
2. All 31 entity files exist: `find backend/src/modules -name "*.entity.ts" | wc -l` returns 31
3. All domain modules export TypeOrmModule: `grep -r "exports.*TypeOrmModule" backend/src/modules/` returns 5
4. Migrations run: `npm run migration:run` succeeds
5. Tables exist: SQL query against information_schema confirms >= 31 tables
6. pgvector columns: kb_chunk.embedding, case_memory.embedding, human_feedback_memory.embedding are vector(1536)
7. Foreign keys: `SELECT count(*) FROM information_schema.table_constraints WHERE constraint_type='FOREIGN KEY'` returns > 0
8. Backend still boots and health check works after migrations
</verification>

<success_criteria>
- 31 TypeORM entity files across 5 domain modules compile without errors
- 5 domain-scoped migration files create all tables in correct dependency order
- PostgreSQL has all ~31 tables with correct columns, types, and foreign keys
- pgvector embedding columns exist on memoria entities
- All domain modules export TypeOrmModule for cross-module access
- Backend boots and /api/health returns ok after migrations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
