---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/src/database/seeds/regulatorio.seeder.ts
  - backend/src/database/seeds/orquestracao.seeder.ts
  - backend/src/database/seeds/complaint-mock.seeder.ts
  - backend/src/database/seeds/main.seeder.ts
  - backend/src/database/seeds/run.ts
  - backend/src/database/factories/complaint.factory.ts
autonomous: true

must_haves:
  truths:
    - "4 tipologias are queryable: cobranca, cancelamento, portabilidade, qualidade"
    - "5 situations are queryable: aberta, reaberta, vencida, em_risco, pedido"
    - "4 regulatory actions are queryable: responder, reclassificar, reencaminhar, cancelar"
    - "Initial regulatory rules from Manual Anatel are queryable"
    - "Mock complaint records exist and are queryable with relations to tipology and situation"
    - "NestJS backend boots, connects to seeded database, and /api/health returns ok"
  artifacts:
    - path: "backend/src/database/seeds/regulatorio.seeder.ts"
      provides: "Seed data for tipologias, situations, regulatory rules, actions, subtipologias"
      contains: "class RegulatorioSeeder"
    - path: "backend/src/database/seeds/complaint-mock.seeder.ts"
      provides: "Mock complaint data injection"
      contains: "class ComplaintMockSeeder"
    - path: "backend/src/database/seeds/main.seeder.ts"
      provides: "Main seeder that orchestrates all seeders in order"
      contains: "class MainSeeder"
    - path: "backend/src/database/seeds/run.ts"
      provides: "Seeder runner script"
      contains: "runSeeders"
  key_links:
    - from: "backend/src/database/seeds/run.ts"
      to: "backend/src/database/data-source.ts"
      via: "imports AppDataSource and initializes"
      pattern: "AppDataSource\\.initialize"
    - from: "backend/src/database/seeds/complaint-mock.seeder.ts"
      to: "tipology and situation tables"
      via: "queries existing tipology/situation to set relations on mock complaints"
      pattern: "getRepository.*Tipology"
---

<objective>
Load seed data (tipologias, situations, regulatory rules, actions) and inject mock complaint data so the database is populated and queryable for development and testing.

Purpose: Without seed data, no regulatory logic can be tested. Without mock complaints, the queue/detail UI in Phase 2 has nothing to display. This plan populates the foundation so every subsequent phase has data to work with.
Output: Regulatory reference data seeded, mock complaints injected, all queryable via SQL and through the NestJS app.
</objective>

<execution_context>
@/Users/maxcardoso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxcardoso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regulatory seed data (tipologias, situations, actions, rules, subtipologias, templates, mandatory info rules)</name>
  <files>
    backend/src/database/seeds/regulatorio.seeder.ts
    backend/src/database/seeds/orquestracao.seeder.ts
    backend/src/database/seeds/main.seeder.ts
    backend/src/database/seeds/run.ts
  </files>
  <action>
    1. Create `backend/src/database/seeds/run.ts` following RESEARCH.md pattern:
       - Import AppDataSource from data-source.ts
       - Import runSeeders from typeorm-extension
       - Initialize datasource, run seeders, destroy datasource
       - Handle errors with console.error and process.exit(1)

    2. Create `backend/src/database/seeds/main.seeder.ts`:
       - Implements Seeder interface from typeorm-extension
       - Calls seeders in dependency order: RegulatorioSeeder first, then OrquestracaoSeeder, then ComplaintMockSeeder
       - Use `call()` helper from typeorm-extension to invoke sub-seeders

    3. Create `backend/src/database/seeds/regulatorio.seeder.ts`:
       Insert the following reference data using repository.upsert() (idempotent by key field):

       **Tipologias (4):**
       - cobranca / "Cobranca" / slaBusinessDays: 10
       - cancelamento / "Cancelamento" / slaBusinessDays: 10
       - portabilidade / "Portabilidade" / slaBusinessDays: 10
       - qualidade / "Qualidade / Reparo" / slaBusinessDays: 10

       **Subtipologias (8+):**
       - Under cobranca: "cobranca_indevida" (Cobranca Indevida), "cobranca_apos_cancelamento" (Cobranca Apos Cancelamento)
       - Under cancelamento: "cancelamento_linha" (Cancelamento de Linha), "cancelamento_combo" (Cancelamento Combo)
       - Under portabilidade: "portabilidade_fixa" (Portabilidade Fixa), "portabilidade_movel" (Portabilidade Movel)
       - Under qualidade: "qualidade_sinal" (Qualidade de Sinal), "qualidade_internet" (Qualidade de Internet)

       **Situations (5):**
       - aberta / "Aberta" / slaOverrideDays: null (uses tipology default)
       - reaberta / "Reaberta" / slaOverrideDays: 5
       - vencida / "Vencida" / slaOverrideDays: null
       - em_risco / "Em Risco" / slaOverrideDays: null
       - pedido / "Pedido de Informacao" / slaOverrideDays: 3

       **Regulatory Actions (4):**
       - responder / "Responder" / requiresJustification: false
       - reclassificar / "Reclassificar" / requiresJustification: true
       - reencaminhar / "Reencaminhar" / requiresJustification: true
       - cancelar / "Cancelar" / requiresJustification: true

       **Regulatory Rules (initial set, ~10 rules):**
       - SLA rules: "SLA_ABERTA_10D" (10 dias para reclamacao aberta), "SLA_REABERTA_5D" (5 dias para reaberta), "SLA_PEDIDO_3D" (3 dias para pedido de informacao)
       - Mandatory field rules: "MANDATORY_PROTOCOL" (numero protocolo obrigatorio), "MANDATORY_CPF" (CPF do reclamante), "MANDATORY_RESPONSE_TEXT" (texto de resposta obrigatorio)
       - Action condition rules: "RECLASSIFICAR_JUSTIFICATIVA" (reclassificacao exige justificativa), "REENCAMINHAR_DESTINATARIO" (reencaminhamento exige destinatario), "CANCELAR_MOTIVO" (cancelamento exige motivo)
       - Blocking rules: "BLOCK_NO_CHECKLIST" (nao pode finalizar sem checklist completo)
       - Link rules to appropriate tipology/situation where applicable (most are null = global)

       **Response Templates (4 — one per tipology):**
       - Template for cobranca with placeholder structure
       - Template for cancelamento with placeholder structure
       - Template for portabilidade with placeholder structure
       - Template for qualidade with placeholder structure
       Each with sourceDocument: "guia_iqi"

       **Mandatory Info Rules (6+):**
       - "numero_protocolo" / "Numero do Protocolo" / required for all
       - "cpf_reclamante" / "CPF do Reclamante" / required for all
       - "nome_reclamante" / "Nome do Reclamante" / required for all
       - "descricao_fato" / "Descricao do Fato" / required for all
       - "providencia_adotada" / "Providencia Adotada" / required for all
       - "data_resolucao" / "Data de Resolucao" / required for all

    4. Create `backend/src/database/seeds/orquestracao.seeder.ts`:
       Insert initial skill definitions (the 19 skills catalog):
       - LoadComplaint, NormalizeComplaintText, ClassifyTypology, ComputeSla, DetermineRegulatoryAction, RetrieveManualContext, RetrieveIQITemplate, BuildMandatoryChecklist, GenerateArtifact, ApplyPersonaTone, DraftFinalResponse, ComplianceCheck, HumanDiffCapture, PersistMemory, TrackTokenUsage, AuditTrail, ValidateReclassification, ValidateReencaminhamento, ValidateCancelamento
       Each with key, name, description, and placeholder input/output schemas.

    5. Update data-source.ts to point seeds to the main seeder:
       Ensure `seeds: ['dist/database/seeds/main.seeder.js']` in dataSourceOptions.

    6. Run: `cd backend && npm run build && node dist/database/seeds/run.js`

    7. Verify via SQL:
       ```sql
       SELECT key, label FROM tipology;
       SELECT key, label FROM situation;
       SELECT key, label FROM regulatory_action;
       SELECT code, title FROM regulatory_rule;
       SELECT key, name FROM skill_definition;
       ```
  </action>
  <verify>
    - `npm run seed` completes without errors
    - `SELECT count(*) FROM tipology` returns 4
    - `SELECT count(*) FROM situation` returns 5
    - `SELECT count(*) FROM regulatory_action` returns 4
    - `SELECT count(*) FROM regulatory_rule` returns >= 10
    - `SELECT count(*) FROM skill_definition` returns 19
    - `SELECT count(*) FROM subtipology` returns >= 8
    - `SELECT count(*) FROM response_template` returns 4
    - `SELECT count(*) FROM mandatory_info_rule` returns >= 6
    - Running seed a second time does not create duplicates (idempotent)
  </verify>
  <done>
    All regulatory reference data is seeded: 4 tipologias, 8+ subtipologias, 5 situations, 4 actions, 10+ regulatory rules, 4 response templates, 6+ mandatory info rules, 19 skill definitions. Seeder is idempotent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create mock complaint data and verify full foundation</name>
  <files>
    backend/src/database/seeds/complaint-mock.seeder.ts
    backend/src/database/factories/complaint.factory.ts
  </files>
  <action>
    1. Create `backend/src/database/factories/complaint.factory.ts`:
       - Use @faker-js/faker with locale pt_BR for Brazilian names, addresses, phone numbers
       - Factory creates Complaint entities with:
         - protocolNumber: sequential format "ANATEL-2024-NNNNNN"
         - rawText: faker-generated complaint text combining tipology context (e.g., "Fui cobrado indevidamente pelo servico...")
         - status: random from ["pending", "in_progress", "waiting_human", "completed"]
         - riskLevel: random from ["low", "medium", "high", "critical"]
         - source: "anatel_portal"
         - slaBusinessDays: 10 (default)
         - slaDeadline: createdAt + slaBusinessDays (calculated)
         - Randomly assign tipology and situation from seeded data

    2. Create `backend/src/database/seeds/complaint-mock.seeder.ts`:
       - Queries existing tipologias and situations from DB (they must already be seeded)
       - Creates 20 mock complaints using the factory, distributed across tipologias:
         - 6 cobranca complaints
         - 5 cancelamento complaints
         - 5 portabilidade complaints
         - 4 qualidade complaints
       - For each complaint, create 3-5 complaint_detail records (customer_name, cpf, phone_number, plan_name, etc. using faker pt_BR)
       - For each complaint, create 1-2 complaint_history records (created, maybe classified)
       - Set some complaints as overdue (isOverdue: true, slaDeadline in the past)
       - Set some as high risk

    3. Update main.seeder.ts to include ComplaintMockSeeder after RegulatorioSeeder and OrquestracaoSeeder.

    4. Run: `cd backend && npm run build && node dist/database/seeds/run.js`

    5. Full foundation verification:
       - Query complaints: `SELECT c.protocol_number, t.label as tipologia, s.label as situacao, c.status FROM complaint c LEFT JOIN tipology t ON c.tipology_id = t.id LEFT JOIN situation s ON c.situation_id = s.id LIMIT 5;`
       - Query details: `SELECT cd.field_name, cd.field_value FROM complaint_detail cd LIMIT 10;`
       - Health check: `curl http://localhost:3001/api/health` returns ok
       - Backend boots with seeded data without errors
  </action>
  <verify>
    - `SELECT count(*) FROM complaint` returns 20
    - `SELECT count(*) FROM complaint_detail` returns >= 60 (3+ per complaint)
    - `SELECT count(*) FROM complaint_history` returns >= 20
    - Complaints have tipology relations: `SELECT count(*) FROM complaint WHERE tipology_id IS NOT NULL` returns 20
    - Complaints have situation relations: `SELECT count(*) FROM complaint WHERE situation_id IS NOT NULL` returns >= 15
    - Some complaints overdue: `SELECT count(*) FROM complaint WHERE is_overdue = true` returns > 0
    - `curl http://localhost:3001/api/health` returns `{"status":"ok","db":true}`
    - `cd frontend && npm run build` still succeeds
  </verify>
  <done>
    20 mock complaints exist with details, history, and relations to tipologias/situations. Mix of statuses, risk levels, and SLA states. Full stack boots: PostgreSQL with pgvector, all tables, seed + mock data, NestJS backend healthy, Next.js frontend builds.
  </done>
</task>

</tasks>

<verification>
Phase 1 complete verification (all 4 success criteria from ROADMAP):

1. "PostgreSQL database with pgvector extension runs and all ~35 tables exist across 5 domains":
   - `docker compose exec postgres psql -U bko -d bkoagent -c "SELECT extname FROM pg_extension WHERE extname='vector'"` returns vector
   - `docker compose exec postgres psql -U bko -d bkoagent -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_type='BASE TABLE'"` returns >= 31

2. "Seed data is loaded: tipologias, situacoes, and regulatory rules are queryable via SQL":
   - `SELECT key FROM tipology` returns cobranca, cancelamento, portabilidade, qualidade
   - `SELECT key FROM situation` returns aberta, reaberta, vencida, em_risco, pedido
   - `SELECT count(*) FROM regulatory_rule` returns >= 10

3. "Mock complaint data from the spreadsheet is injected and queryable":
   - `SELECT count(*) FROM complaint` returns 20
   - `SELECT c.protocol_number, t.label FROM complaint c JOIN tipology t ON c.tipology_id = t.id LIMIT 5` returns data

4. "NestJS backend and Next.js frontend boot without errors and can reach the database":
   - `curl http://localhost:3001/api/health` returns `{"status":"ok","db":true}`
   - `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- Regulatory reference data seeded: 4 tipologias, 5 situations, 4 actions, 10+ rules, 19 skills
- 20 mock complaints with details, history, and tipology/situation relations
- Seeders are idempotent (re-running does not create duplicates)
- Full stack boots and /api/health returns ok
- All Phase 1 success criteria from ROADMAP satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
