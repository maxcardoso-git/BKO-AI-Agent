import { Module } from '@nestjs/common';

/**
 * Orquestracao module — capability, capability_version, step_definition,
 * step_transition_rule, skill_definition, step_skill_binding
 * Entities and services will be added in Plan 02 (domain schema).
 */
@Module({
  imports: [],
  exports: [],
})
export class OrquestracaoModule {}
